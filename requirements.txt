numpy
scipy
pandas
matplotlib
seaborn
xarray==2025.10.1
h5netcdf
netCDF4
cftime
scikit-learn
tqdm
IPython
pytest
argopy
earthaccess
gsw
bokeh
healpy

# Special-source packages (not added above to keep `pip install -r` from
# failing). Install separately for now:
#   bing            -- being packaged for PyPI; until then install from source
#   remote_sensing  -- pip install git+https://github.com/<owner>/remote_sensing
#   ocpy            -- pip install git+https://github.com/<owner>/ocpy  (or local checkout)
