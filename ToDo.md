# ToDo

- [ ] Implement parallel `fit_batch` across cores
- [ ] Bump `pab_version` from `0.0.dev0` to `1.0`
- [ ] Keep chains/figures local and publish only the report + summary tables
- [ ] Create a Nautilus namespace + S3 bucket
- [ ] Create a new Namespace for this on Nautilus.  And its own s3 bucket.
- [ ] Implement parallel `fit_batch` across cores
- [ ] Bump `pab_version` from `0.0.dev0` to `1.0`
- [ ] Keep chains/figures local and publish only the report + summary tables
- [ ] Create S3 bucket
- [ ] Rerun the Jacqueline S (MBARI) analysis