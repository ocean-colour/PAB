"""Chl-a deep-dive figure set (chl_cdom_prompt_2.md Task 1).

Standalone analysis scripts over ``pab.db`` — not part of the ``pab`` package's
report pipeline, not published to Read the Docs. Every script starts from
:func:`pab.matchup.chl.data.load_chl_matchups`; see :mod:`pab.matchup.chl.run_all`
to reproduce the full set in one command.
"""
